// Update this file: build.gradle (project level)
buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.6.0' // Updated from 8.1.0 to 8.6.0
    }
}

// Bloc ajouté pour fixer les versions du SDK Android
ext {
    compileSdkVersion = 35
    targetSdkVersion = 35
    minSdkVersion = 21
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"

subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}

subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}